import { forwardRef } from 'react';
import type { Ref } from 'react';

import type { FormProps, FormRef } from '../form';

const Form = forwardRef(function Form(_props: FormProps, _ref: Ref<FormRef>) {
  return null;
});

export default Form;