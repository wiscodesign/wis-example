import type { ToggleProps } from "../toggle";
declare function Toggle({ className, variant, disabled, text, icon, iconControl, size, shortcutKey, pressed, defaultPressed, onPressed, ...rest }: ToggleProps): import("react/jsx-runtime").JSX.Element;
declare namespace Toggle {
    var displayName: string;
}
export default Toggle;
