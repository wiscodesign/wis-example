import type { AvatarProps } from "../avatar";
declare function Avatar({ className, icon, src, size, color, colorSchema, shape, name, initials, ...rest }: AvatarProps): import("react/jsx-runtime").JSX.Element;
declare namespace Avatar {
    var displayName: string;
}
export default Avatar;
