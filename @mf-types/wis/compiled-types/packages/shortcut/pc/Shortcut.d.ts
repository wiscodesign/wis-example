import type { ShortcutProps } from "../shortcut";
declare function Shortcut({ className, shortcutKey, readonly, disabled, variant, size, onKeyPressed, ...rest }: ShortcutProps): import("react/jsx-runtime").JSX.Element | null;
declare namespace Shortcut {
    var displayName: string;
}
export default Shortcut;
